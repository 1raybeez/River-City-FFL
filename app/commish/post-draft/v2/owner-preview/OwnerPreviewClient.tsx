"use client";

import { useMemo, useState } from "react";
import { buildOwnerDraftVerdict } from "@/lib/draftReportV2/ownerVerdict";

function rank(value: number | undefined) { return value == null ? "—" : `#${value}`; }
function summaryLabel(value: string | undefined) { return value?.replace(" — ", " · ") ?? "—"; }

function Metric({ label, value, large = false }: { label: string; value: string; large?: boolean }) {
  return <div><p className="text-[10px] font-black uppercase tracking-[0.14em] text-slate-500">{label}</p><p className={large ? "mt-1 text-2xl font-black leading-none text-slate-950" : "mt-1 font-bold text-slate-800"}>{value}</p></div>;
}

function PlayerList({ title, players }: { title: string; players: any[] }) {
  if (!players.length) return null;
  return <section><h3 className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">{title}</h3><div className="mt-2 grid gap-2 sm:grid-cols-2">{players.map((player) => <div key={`${title}-${player.playerId}`} className="rounded-lg border border-slate-200 bg-white px-3 py-2"><p className="break-normal font-bold text-slate-900">{player.playerName}</p><p className="mt-0.5 text-xs text-slate-500">{player.position ?? "Position not listed"}</p></div>)}</div></section>;
}

function PlayerStory({ story }: { story: { startingCore: any[]; positionRoom: any[]; usefulDepth: any[]; flexContext: any[] } }) {
  return <section className="mt-8 border-t border-slate-200 pt-6"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-orange-700">Your player story</p><h2 className="mt-1 text-2xl font-black tracking-tight text-slate-950">The draft pieces behind your grade</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">These players were identified by the frozen draft-night evaluation as part of your starting core, position-room depth, or useful roster depth. This is context for the report, not a separate player grade.</p></div><div className="mt-5 grid gap-5 md:grid-cols-2"><PlayerList title="STARTING CORE" players={story.startingCore} /><PlayerList title="POSITION CONTRIBUTORS" players={story.positionRoom} /><PlayerList title="USEFUL DEPTH" players={story.usefulDepth} /><PlayerList title="FLEX CONTEXT" players={story.flexContext} /></div></section>;
}

function OwnerReport({ report }: { report: any }) {
  const { team, formula, quality, auction, final } = report;
  const verdict = buildOwnerDraftVerdict({ teamName: team.currentDisplayName, overallRank: final.overallRank, grade: final.grade, draftQualityRank: formula.draftQualityRank, auctionEfficiencyRank: auction.auctionEfficiency.leagueRank, startingLineupRank: quality.components.startingLineup.leagueRank, usefulDepthRank: quality.components.depth.leagueRank, biggestStrength: quality.biggestStrength, biggestWeakness: quality.biggestWeakness });
  return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><header className="border-b-4 border-orange-500 bg-slate-950 px-5 py-6 text-white sm:px-8"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-300">2026 Draft Report Card</p><div className="mt-4 flex flex-wrap items-end justify-between gap-5"><div><h1 className="text-3xl font-black tracking-tight sm:text-4xl">{team.currentDisplayName}</h1>{team.historicalDraftTimeTeamName !== team.currentDisplayName && <p className="mt-2 text-sm font-medium text-slate-300">Drafted as: {team.historicalDraftTimeTeamName}</p>}<p className="mt-4 text-sm text-slate-300">A league-relative look at the roster you left the auction with.</p></div><div className="text-left sm:text-right"><p className="text-7xl font-black leading-none text-orange-400" aria-label={`Grade ${final.grade}`}>{final.grade}</p><p className="mt-2 text-lg font-bold text-white">{final.methodDScore.toFixed(2)} score</p><p className="mt-1 text-sm font-bold text-slate-300">Overall {rank(final.overallRank)} in River City FFL</p></div></div></header><div className="px-5 py-6 sm:px-8"><section className="rounded-xl border border-orange-200 bg-orange-50/70 p-4 sm:p-5" aria-labelledby="verdict-heading"><p className="text-xs font-black uppercase tracking-[0.16em] text-orange-700">THE VERDICT</p><h2 id="verdict-heading" className="mt-1 text-xl font-black tracking-tight text-slate-950">The story behind your grade</h2><p className="mt-3 max-w-4xl text-sm leading-6 text-slate-700">{verdict}</p></section><section className="mt-7" aria-labelledby="report-summary-heading"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-orange-700">Report summary</p><h2 id="report-summary-heading" className="mt-1 text-2xl font-black tracking-tight text-slate-950">How your draft performed</h2></div><p className="text-xs text-slate-500">Compared with the other 2026 River City teams</p></div><div className="mt-5 grid grid-cols-2 gap-4 border-y border-slate-200 py-5 sm:grid-cols-4"><Metric label="Draft Quality rank" value={rank(formula.draftQualityRank)} large /><Metric label="Auction Efficiency rank" value={rank(auction.auctionEfficiency.leagueRank)} large /><Metric label="Starting Lineup rank" value={rank(quality.components.startingLineup.leagueRank)} /><Metric label="Useful Depth rank" value={rank(quality.components.depth.leagueRank)} /></div></section><section className="mt-7" aria-labelledby="position-report-heading"><p className="text-xs font-black uppercase tracking-[0.16em] text-orange-700">Position report</p><h2 id="position-report-heading" className="mt-1 text-2xl font-black tracking-tight text-slate-950">Where your roster stood</h2><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric label="Quarterback" value={rank(quality.components.qbRoom.leagueRank)} large /><Metric label="Running back" value={rank(quality.components.rbRoom.leagueRank)} large /><Metric label="Wide receiver" value={rank(quality.components.wrRoom.leagueRank)} large /><Metric label="Tight end" value={rank(quality.components.teRoom.leagueRank)} large /></div><p className="mt-3 text-xs text-slate-500">FLEX is considered as roster context, not as an independent grade component.</p></section><section className="mt-7 grid gap-4 sm:grid-cols-2" aria-label="Draft strengths"><div className="rounded-xl border-l-4 border-orange-500 bg-orange-50 p-4"><p className="text-xs font-black uppercase tracking-[0.14em] text-orange-700">Biggest strength</p><p className="mt-2 font-black text-slate-950">{summaryLabel(quality.biggestStrength)}</p></div><div className="rounded-xl border-l-4 border-slate-400 bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-[0.14em] text-slate-600">Needs work</p><p className="mt-2 font-black text-slate-950">{summaryLabel(quality.biggestWeakness)}</p></div></section><PlayerStory story={report.story} /><section className="mt-8 grid gap-4 border-t border-slate-200 pt-6 sm:grid-cols-2"><div><p className="text-xs font-black uppercase tracking-[0.14em] text-orange-700">Auction execution</p><h2 className="mt-1 text-xl font-black text-slate-950">Using your auction budget</h2><p className="mt-2 text-sm leading-6 text-slate-600">Auction Efficiency shows how effectively your draft-night spending supported your roster compared with the league. Your league rank was <span className="font-bold text-slate-900">{rank(auction.auctionEfficiency.leagueRank)}</span>.</p></div><div><details className="rounded-xl border border-slate-200 bg-slate-50 p-4"><summary className="cursor-pointer font-black text-slate-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-500">HOW YOUR GRADE WORKS</summary><div className="mt-3 space-y-2 text-sm leading-6 text-slate-600"><p>This is a frozen evaluation of your completed draft-night roster.</p><p>Draft Quality is 85% of the grade and Auction Efficiency is 15%.</p><p>Draft Quality emphasizes your starting lineup, position groups, and useful depth.</p><p>Your result is compared with the other 2026 River City teams. It is league-relative, not an absolute cross-season rating.</p></div></details></div></section><footer className="mt-8 border-t border-slate-200 pt-5 text-xs leading-5 text-slate-500">This report evaluates the roster you left the auction with. Trades, waivers, injuries, and later roster changes do not change this grade.</footer></div></article>;
}

export default function OwnerPreviewClient({ reports, selectedFranchiseId }: { reports: any[]; selectedFranchiseId: string }) {
  const [franchiseId, setFranchiseId] = useState(selectedFranchiseId);
  const report = useMemo(() => reports.find((candidate) => candidate.team.franchiseId === franchiseId) ?? reports[0], [franchiseId, reports]);
  return <><header className="mb-5"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-700">Commissioner-only preview</p><h1 className="mt-1 text-2xl font-black tracking-tight text-slate-950">Owner Draft Report Card V2</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Preview the report an owner would eventually see. This preview is not published and remains separate from the commissioner diagnostic report.</p><label className="mt-4 flex max-w-md flex-col gap-2 text-xs font-black uppercase tracking-[0.14em] text-slate-600">Preview team<select value={report.team.franchiseId} onChange={(event) => setFranchiseId(event.target.value)} className="min-h-11 rounded-lg border border-slate-300 bg-white px-3 text-sm font-bold normal-case tracking-normal text-slate-900 shadow-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-500">{reports.map((candidate) => <option key={candidate.team.franchiseId} value={candidate.team.franchiseId}>{rank(candidate.final.overallRank)} · {candidate.team.currentDisplayName}</option>)}</select></label></header><OwnerReport report={report} /></>;
}
