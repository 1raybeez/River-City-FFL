import type { PredictionCoverageState, PredictionFranchiseInput, PredictionInputSnapshot, PredictionRosterPlayer } from "@/lib/predictions/inputSnapshot";

export const PREDICTION_STRENGTH_MODEL_VERSION = "preseason-strength-v1" as const;
export const PREDICTION_COMPONENT_WEIGHTS = { starters: 0.6, depth: 0.25, positionalBalance: 0.15 } as const;
type CorePosition = "QB" | "RB" | "WR" | "TE";
type ComponentName = "starters" | "depth" | "positionalBalance" | "qb" | "rb" | "wr" | "te" | "flex";
const CORE_POSITIONS: readonly CorePosition[] = ["QB", "RB", "WR", "TE"];
const FLEX_POSITIONS = new Set(["RB", "WR", "TE"]);
const STARTER_COUNTS: Record<CorePosition, number> = { QB: 1, RB: 2, WR: 2, TE: 1 };
const STARTER_ORDER = ["QB", "RB1", "RB2", "WR1", "WR2", "TE", "FLEX"] as const;
const EXPLANATION_COMPONENTS: readonly [string, ComponentName][] = [["starting lineup", "starters"], ["bench/depth", "depth"], ["QB room", "qb"], ["RB room", "rb"], ["WR room", "wr"], ["TE room", "te"], ["FLEX options", "flex"]];

export type PredictionStrengthConfidence = "HIGH" | "MEDIUM" | "LOW";
export type PredictionStrengthTier = "TITLE CONTENDER" | "CONTENDER" | "PLAYOFF CALIBER" | "BUBBLE" | "LONG SHOT";
export type PredictionStrengthComponent = { score: number | null; leagueRank: number | null; coveredPlayers: number; totalPlayers: number; coverage: PredictionCoverageState };
export type PredictionLineupSlot = { slot: string; playerId: string | null; playerName: string | null; evidenceScore: number | null };
export type PredictionTeamStrength = { modelVersion: typeof PREDICTION_STRENGTH_MODEL_VERSION; franchiseId: string; canonicalTeamName: string; teamName: string; strengthScore: number; leagueRelativeRank: number; confidence: PredictionStrengthConfidence; tier: PredictionStrengthTier; rawComponents: { starters: number | null; depth: number | null; positionalBalance: number | null }; weightedContributions: { starters: number; depth: number; positionalBalance: number }; rawWeightedComposite: number; startingLineup: readonly PredictionLineupSlot[]; components: { starters: PredictionStrengthComponent; depth: PredictionStrengthComponent; positionalBalance: PredictionStrengthComponent; qb: PredictionStrengthComponent; rb: PredictionStrengthComponent; wr: PredictionStrengthComponent; te: PredictionStrengthComponent; flex: PredictionStrengthComponent }; biggestStrength: string; biggestWeakness: string; coverage: PredictionFranchiseInput["coverage"]; reasonCodes: readonly string[] };
export type PredictionStrengthReport = { modelVersion: typeof PREDICTION_STRENGTH_MODEL_VERSION; generatedAt: string; evidenceFreshness: PredictionInputSnapshot["evidenceFreshness"]; weights: typeof PREDICTION_COMPONENT_WEIGHTS; normalization: string; teams: readonly PredictionTeamStrength[] };
type InternalPlayer = PredictionRosterPlayer & { evidenceScore: number | null };
type PreliminaryTeam = Omit<PredictionTeamStrength, "strengthScore" | "leagueRelativeRank" | "tier" | "biggestStrength" | "biggestWeakness"> & { biggestStrength: string; biggestWeakness: string };

const roundOne = (value: number) => Math.round(value * 10) / 10;
const average = (values: readonly number[]) => values.length ? roundOne(values.reduce((sum, value) => sum + value, 0) / values.length) : null;
function component(score: number | null, players: readonly InternalPlayer[]): PredictionStrengthComponent { const coveredPlayers = players.filter((player) => player.evidenceScore !== null).length; return { score, leagueRank: null, coveredPlayers, totalPlayers: players.length, coverage: coveredPlayers === 0 ? "MISSING" : coveredPlayers === players.length ? "COMPLETE" : "PARTIAL" }; }
function percentile(value: number, values: readonly number[]) { if (values.length <= 1) return 50; const lower = values.filter((candidate) => candidate < value).length; const equal = values.filter((candidate) => candidate === value).length; return roundOne(((lower + (equal - 1) / 2) / (values.length - 1)) * 100); }
function minMax(value: number, values: readonly number[]) { const minimum = Math.min(...values); const maximum = Math.max(...values); return maximum === minimum ? 50 : roundOne(((value - minimum) / (maximum - minimum)) * 100); }
function rankOf(value: number | null, values: readonly (number | null)[]) { return value === null ? null : values.filter((candidate): candidate is number => candidate !== null && candidate > value).length + 1; }
function confidence(franchise: PredictionFranchiseInput): PredictionStrengthConfidence { const players = franchise.rosterPlayers; if (!players.length) return "LOW"; const covered = players.filter((player) => player.evidence.fantasyCalc !== null || player.evidence.ros !== null).length / players.length; const core = players.filter((player) => CORE_POSITIONS.includes(player.position as CorePosition)); const coreCovered = core.filter((player) => player.evidence.fantasyCalc !== null || player.evidence.ros !== null).length / Math.max(1, core.length); return covered >= 0.8 && coreCovered >= 0.8 ? "HIGH" : covered >= 0.5 && coreCovered >= 0.5 ? "MEDIUM" : "LOW"; }
function tier(rank: number, count: number): PredictionStrengthTier { if (rank <= Math.max(1, Math.ceil(count * 2 / 12))) return "TITLE CONTENDER"; if (rank <= Math.max(2, Math.ceil(count * 4 / 12))) return "CONTENDER"; if (rank <= Math.max(4, Math.ceil(count * 8 / 12))) return "PLAYOFF CALIBER"; if (rank <= Math.max(6, Math.ceil(count * 10 / 12))) return "BUBBLE"; return "LONG SHOT"; }
function normalizePlayers(snapshot: PredictionInputSnapshot): Map<string, InternalPlayer> { const all = snapshot.franchises.flatMap((franchise) => franchise.rosterPlayers); const byPosition = new Map<string, InternalPlayer[]>(); all.forEach((player) => { const rows = byPosition.get(player.position ?? "UNKNOWN") ?? []; rows.push({ ...player, evidenceScore: null }); byPosition.set(player.position ?? "UNKNOWN", rows); }); for (const rows of byPosition.values()) { const values = rows.flatMap((player) => player.evidence.fantasyCalc?.value ?? []); const ranks = rows.flatMap((player) => player.evidence.fantasyCalc === null ? player.evidence.ros?.positionalRank ?? [] : []); rows.forEach((player) => { if (player.evidence.fantasyCalc) player.evidenceScore = percentile(player.evidence.fantasyCalc.value, values); else if (player.evidence.ros?.positionalRank !== null && player.evidence.ros?.positionalRank !== undefined) player.evidenceScore = percentile(-player.evidence.ros.positionalRank, ranks.map((rank) => -rank)); }); } return new Map(all.map((player) => [player.sleeperPlayerId, { ...player, evidenceScore: byPosition.get(player.position ?? "UNKNOWN")?.find((candidate) => candidate.sleeperPlayerId === player.sleeperPlayerId)?.evidenceScore ?? null }])); }
function chooseBest(candidates: readonly InternalPlayer[], count: number, used: Set<string>) { return [...candidates].filter((player) => !used.has(player.sleeperPlayerId)).sort((a, b) => (b.evidenceScore ?? -1) - (a.evidenceScore ?? -1) || a.sleeperPlayerId.localeCompare(b.sleeperPlayerId)).slice(0, count); }
function optimizeLineup(players: readonly InternalPlayer[]) { const used = new Set<string>(); const lineup: PredictionLineupSlot[] = []; for (const position of CORE_POSITIONS) chooseBest(players.filter((player) => player.position === position), STARTER_COUNTS[position], used).forEach((player, index) => { used.add(player.sleeperPlayerId); lineup.push({ slot: `${position}${index + 1}`, playerId: player.sleeperPlayerId, playerName: player.displayName, evidenceScore: player.evidenceScore }); }); const flex = chooseBest(players.filter((player) => FLEX_POSITIONS.has(player.position ?? "")), 1, used)[0] ?? null; if (flex) { used.add(flex.sleeperPlayerId); lineup.push({ slot: "FLEX", playerId: flex.sleeperPlayerId, playerName: flex.displayName, evidenceScore: flex.evidenceScore }); } return { lineup, used }; }
function buildTeam(franchise: PredictionFranchiseInput, players: readonly InternalPlayer[]): PreliminaryTeam { const { lineup, used } = optimizeLineup(players); const starters = lineup.flatMap((slot) => slot.playerId ? players.filter((player) => player.sleeperPlayerId === slot.playerId) : []); const depthPlayers = CORE_POSITIONS.flatMap((position) => chooseBest(players.filter((player) => player.position === position), 2, new Set(used))); const group = (position: CorePosition) => { const candidates = players.filter((player) => player.position === position); return component(average(chooseBest(candidates, STARTER_COUNTS[position], new Set()).flatMap((player) => player.evidenceScore ?? [])), candidates); }; const qb = group("QB"); const rb = group("RB"); const wr = group("WR"); const te = group("TE"); const flexPlayers = players.filter((player) => FLEX_POSITIONS.has(player.position ?? "")); const starter = component(average(starters.flatMap((player) => player.evidenceScore ?? [])), starters); const depth = component(average(depthPlayers.flatMap((player) => player.evidenceScore ?? [])), depthPlayers); const positionalBalance = average([qb.score, rb.score, wr.score, te.score].filter((value): value is number => value !== null)); const weightedContributions = { starters: roundOne((starter.score ?? 50) * PREDICTION_COMPONENT_WEIGHTS.starters), depth: roundOne((depth.score ?? 50) * PREDICTION_COMPONENT_WEIGHTS.depth), positionalBalance: roundOne((positionalBalance ?? 50) * PREDICTION_COMPONENT_WEIGHTS.positionalBalance) }; return { modelVersion: PREDICTION_STRENGTH_MODEL_VERSION, franchiseId: franchise.franchiseId, canonicalTeamName: franchise.canonicalTeamName, teamName: franchise.currentTeamName, confidence: confidence(franchise), rawComponents: { starters: starter.score, depth: depth.score, positionalBalance }, weightedContributions, rawWeightedComposite: roundOne(weightedContributions.starters + weightedContributions.depth + weightedContributions.positionalBalance), startingLineup: STARTER_ORDER.map((slot) => lineup.find((candidate) => candidate.slot === slot) ?? { slot, playerId: null, playerName: null, evidenceScore: null }), components: { starters: starter, depth, positionalBalance: component(positionalBalance, players.filter((player) => CORE_POSITIONS.includes(player.position as CorePosition))), qb, rb, wr, te, flex: component(average(chooseBest(flexPlayers, 1, new Set()).flatMap((player) => player.evidenceScore ?? [])), flexPlayers) }, biggestStrength: "evidence coverage", biggestWeakness: "evidence coverage", coverage: franchise.coverage, reasonCodes: ["FANTASYCALC_FIRST_ROS_FALLBACK", "UNKNOWN_EVIDENCE_EXCLUDED_FROM_COMPONENT_AVERAGES", "K_DEF_EXCLUDED_FROM_STRENGTH"] }; }

export function buildPredictionStrengthReport(snapshot: PredictionInputSnapshot): PredictionStrengthReport { const playerMap = normalizePlayers(snapshot); const preliminary = snapshot.franchises.map((franchise) => buildTeam(franchise, franchise.rosterPlayers.map((player) => playerMap.get(player.sleeperPlayerId)!).filter(Boolean))); const rawScores = preliminary.map((team) => team.rawWeightedComposite); const ranked = preliminary.map((team) => ({ team, strengthScore: minMax(team.rawWeightedComposite, rawScores) })).sort((a, b) => b.strengthScore - a.strengthScore || b.team.rawWeightedComposite - a.team.rawWeightedComposite || a.team.franchiseId.localeCompare(b.team.franchiseId)); const componentNames: readonly ComponentName[] = ["starters", "depth", "positionalBalance", "qb", "rb", "wr", "te", "flex"]; const componentValues = Object.fromEntries(componentNames.map((name) => [name, ranked.map(({ team }) => team.components[name].score)])) as unknown as Record<ComponentName, readonly (number | null)[]>; const teams = ranked.map(({ team, strengthScore }, index) => { const components = Object.fromEntries(componentNames.map((name) => [name, { ...team.components[name], leagueRank: rankOf(team.components[name].score, componentValues[name]) }])) as typeof team.components; const strongest = [...EXPLANATION_COMPONENTS].sort((a, b) => (components[a[1]].leagueRank ?? Number.POSITIVE_INFINITY) - (components[b[1]].leagueRank ?? Number.POSITIVE_INFINITY) || (components[b[1]].score ?? -1) - (components[a[1]].score ?? -1) || EXPLANATION_COMPONENTS.indexOf(a) - EXPLANATION_COMPONENTS.indexOf(b))[0] ?? ["evidence coverage", "starters" as const]; const weakest = [...EXPLANATION_COMPONENTS].sort((a, b) => (components[b[1]].leagueRank ?? Number.NEGATIVE_INFINITY) - (components[a[1]].leagueRank ?? Number.NEGATIVE_INFINITY) || (components[a[1]].score ?? Number.POSITIVE_INFINITY) - (components[b[1]].score ?? Number.POSITIVE_INFINITY) || EXPLANATION_COMPONENTS.indexOf(a) - EXPLANATION_COMPONENTS.indexOf(b))[0] ?? ["evidence coverage", "starters" as const]; return { ...team, strengthScore, leagueRelativeRank: index + 1, tier: tier(index + 1, ranked.length), components, biggestStrength: explanation(strongest[0], components[strongest[1]].score, components[strongest[1]].leagueRank, ranked.length), biggestWeakness: explanation(weakest[0], components[weakest[1]].score, components[weakest[1]].leagueRank, ranked.length) }; }); return { modelVersion: PREDICTION_STRENGTH_MODEL_VERSION, generatedAt: snapshot.generatedAt, evidenceFreshness: snapshot.evidenceFreshness, weights: PREDICTION_COMPONENT_WEIGHTS, normalization: "Raw weighted composite = 60% optimized starters + 25% capped core depth + 15% QB/RB/WR/TE positional balance. Final strength score = min-max normalization across the current league: (composite - minimum) / (league maximum - league minimum) × 100; equal composites receive 50. It is not rank percentile or win probability.", teams }; }
function explanation(label: string, score: number | null, rank: number | null, count: number) { return score === null || rank === null ? `${label} evidence unavailable` : `${label} ${roundOne(score)}/100 (league rank ${rank}/${count})`; }
