// /lib/timeline/leagueTypes.ts

export interface LeagueData {
  teamCount: number;        // total teams in league
  avgPointsFor: number;     // league-wide PF average
  avgRosterAge: number;     // league-wide roster age average
}
